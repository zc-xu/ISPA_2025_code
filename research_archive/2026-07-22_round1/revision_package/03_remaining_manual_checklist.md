# 投稿前人工确认清单

## 已完成并可直接检查的内容

- 已从第一次投稿 TeX 重建标记稿和净稿，原始文件未覆盖。
- 标记稿只对实际改动显示蓝色；净稿源码不含 `revision`、`rev` 或蓝色标记命令。
- Fig. 1 已使用指定 Visio 第 2 页并裁除最外围导出边框，保持单栏宽度；开放服务决策槽位、服务色块和用户颜色均与正文一致。
- Fig. 2 的独立 `Evolutionary Optimization` 面板已按参考图 `513×253`、约 `2.03:1` 的比例重画为 PDF/SVG/PNG，箭头无遮挡、文字不超框，公式使用正常上下标。
- CLS 初始化敏感性图已合并为单栏图，子图标题位于图下方；两个面板之间已增加间距，随机与贪心柱均不显示误差棒。
- Stage-II 两组柱形图已纳入 DQN，并保持 PSP 在两组共 8 个图示场景中均为最低 Q；其中包含 7 个唯一配置。方法名称直接显示在横轴，冗余共享图例已删除，整体尺寸与 Stage-I 多面板图统一。
- Stage-II 柱形图已去除柱顶数值和误差棒；原 Excel 的 `stdErr` 是对同图五种方法横向计算的标准误，不代表独立重复运行的不确定性，因此不用于论文图。
- `spreadsheets/stage2_five_method_results_editable.xlsx` 已归档两组数值、八张可编辑原生柱图和自动核验公式；核验页确认 PSP 在 7 个唯一配置中均为最低 Q。
- HV、IGD、Best Q 表格已经入稿；Spacing 未入稿。
- Stage-II Pareto 图恢复为第一次投稿所用的四幅原始数据 PDF。
- 原始带宽、发射功率、噪声和信道增益模型已保留。
- 小规模联合优化和真实区域泛化结果已写入回信初稿，不进入论文正文。

## 投稿前必须由作者确认

1. **作者信息**  
   核对作者顺序、单位、邮箱、通讯作者脚注和基金信息是否与投稿系统完全一致。

2. **参考文献最终元数据**  
   已新增 Reviewer 推荐列表中的四篇相关文献 [1]、[3]、[4] 和 [5]。卷期页码已按出版元数据补齐；请最终确认目标期刊是否要求在参考文献中显示 DOI。列表中的 [2] 和 [6] 均为 FSO-enabled SAGIN 任务卸载研究，回信中已说明其问题设置与本文不同。

3. **Fig. 2 手工合成**  
   将 `figures/fig2_evolutionary_optimization_panel_compact.svg` 或同名 PDF 贴入原始 Visio 的 `Evolutionary Optimization` 区域。该面板严格按 513×253 比例输出；完成手工合成后再检查整图连接箭头和外围边框。导出最终整图后，将其命名为 `algo_0421.pdf` 覆盖 `manuscript/` 中的稳定占位版本，再在 Overleaf 编译检查。原 Visio 文件和备份均未覆盖。

4. **回信附件范围**  
   联合精确枚举和真实区域泛化目前以详细表格写在回信中。确认是否同时提交对应 CSV、Excel 或单独补充材料；论文正文无需因此增加篇幅。

5. **DQN 复现实验归档**  
   正文只保留两句方法概述；状态、动作、奖励、网络结构、训练回合和超参数保留在回信与复现材料中。投稿前核对最终脚本、配置和结果 CSV 已一并归档。

6. **Overleaf 全项目编译**  
   本地 Tectonic 已编译通过。上传时需要同时上传所有新版 PDF 图件、`IEEEtran.cls` 和参考资源，并在 Overleaf 上做一次完整编译，确认其 TeX Live 版本没有改变浮动体位置。

7. **页数与期刊格式**  
   当前标记稿和净稿均为 14 页。确认重投稿的页数政策、补充材料政策和自愿/强制版面费规则。

8. **最终视觉审查**  
   建议重点人工查看 Fig. 1、手工合成后的 Fig. 2、两组 Stage-II 柱形图、Pareto fronts、Fig. 9 和 Table III；这些是本轮最主要的新增或重排内容。

## 按当前决策不进入正文的内容

- `varpi_j` 敏感性实验：正文采用比例式解释 `varpi_j=ceil(0.5 V_j)`。
- 小规模联合优化数值：只进入回信。
- 真实区域泛化实验：只进入回信。
- 可靠性实验：不新增，正文给出约束扩展方式并列入 future work。
- Spacing 指标：不展示，保留 HV、IGD 和 Best Q。
- SPEA2：不展示。
