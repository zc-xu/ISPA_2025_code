# MOS2 Revision Handoff, 2026-07-19

## Scope of this handoff

This handoff records the restoration of the original Stage-II paper results, the corrected DQN fifth-bar comparison, and the manuscript/response synchronization completed after the 2026-07-17 repository update. No commit or push was performed for this round.

## Key conclusions

- The original submitted main-run seed is `42`, not `44`.
- The authoritative four-method scale-sweep values are those stored in the original paper Excel workbook.
- PSP has the lowest Best Q in all eight displayed panels, corresponding to seven distinct configurations.
- Relative to the best alternative evolutionary initialization in each configuration, PSP reduces Best Q by `2.26%--9.53%`, with a mean reduction of `4.11%`.
- The corrected DQN value for `5 servers / 130 users` is `0.4052`; the previous `0.4751` used later rerun bounds and is not used in the paper comparison.
- In the representative `10 servers / 130 users` case, PSP obtains `HV=0.9470`, `IGD=0.0016`, and `Best Q=0.3282`; DQN obtains `Best Q=0.6125`.

## Code and data added

| File | Purpose |
|---|---|
| `LocalSearch/audit_original_stage2_protocol.py` | Replays the submitted method order and audits candidate seeds/data variants. |
| `LocalSearch/build_stage2_paper_comparison.py` | Combines the original paper archive with real DQN outputs, validates PSP's ranking, and exports CSV/JSON evidence. |
| `LocalSearch/plot_stage2_five_method.py` | Rebuilds the two paper-style five-method scale-sweep figures. |
| `LocalSearch/build_stage2_five_method_workbook.mjs` | Rebuilds the editable Excel comparison from the archived template. |
| `data/paper_archive/stage2_bestq_original_paper.csv` | Machine-readable transcription of the original Excel scale-sweep bars. |
| `data/paper_archive/stage2_original_paper_template.xlsx` | Exact SHA-256-matched copy of the original paper chart workbook. |
| `output/csv/stage2_bestq_original_with_dqn.csv` | Final five-method plot/workbook source. |
| `output/csv/stage2_bestq_original_with_dqn_validation.csv` | Per-configuration winner and PSP reduction audit. |
| `docs/revision/03_experiment_baselines/stage2_original_paper_dqn_audit.md` | Full data provenance and result explanation. |

## Manuscript artifacts

The working paper project from that round is preserved under
`research_archive/2026-07-22_round1/revision_package/manuscript/`.

Updated files:

- `conference_101719_round1_marked(1).tex`
- `conference_101719_round1_clean.tex`
- `stage2_fixed_servers_with_dqn.pdf`
- `stage2_fixed_users_with_dqn.pdf`

The revised text now states that PSP is lowest in every scale-sweep panel and reports the `2.26%--9.53%` range and `4.11%` mean reduction. Six directly colored equation blocks were changed to obey the revision toggle, so the marked manuscript colors only revisions and the clean manuscript renders entirely in black.

Compiled PDFs:

- `D:/EdgeComputing_journal/build_stage2_original_marked/conference_101719_round1_marked(1).pdf`
- `D:/EdgeComputing_journal/build_stage2_original_clean/conference_101719_round1_clean.pdf`

Both compile successfully with Tectonic and contain 12 pages. Pages containing the two DQN figures and the final result discussion were rendered and visually checked.

## Reviewer-response and audit artifacts

- `D:/EdgeComputing_journal/outputs/reviewer_response_ready_en_cn.md`
- `D:/EdgeComputing_journal/outputs/revision_items/00_index.md`
- `D:/EdgeComputing_journal/outputs/revision_items/R2_07_dqn_baseline.md`
- `D:/EdgeComputing_journal/outputs/revision_items/DATA_11_stage2_original_results.md`
- `D:/EdgeComputing_journal/outputs/revision_items/MANUAL_12_remaining_items.md`
- `D:/EdgeComputing_journal/outputs/manuscript_revision_audit_cn.md`

The response text is written in submission-ready language and no longer contains workflow placeholders such as “current status,” “after re-exporting,” or “still requires.” The separate manual checklist records incomplete figure-source work.

## Remaining manual work

1. Redraw Fig. 1 so all visual elements match the revised caption.
2. Redraw the Fig. 2 capacity path as `Round -> Capacity check -> Repair -> Evaluate`.
3. Re-export the remaining legacy figures after checking font sizes at final layout dimensions.
4. Confirm the IEEE Internet of Things Journal template; the working TeX still uses IEEE conference mode.
5. Perform the final Overleaf compile and verify cross-references, author metadata, funding, bibliography, and figure placement.
6. If the historical raw arrays for `10_150`, `10_180`, `15_130`, and `20_130` are recovered, rerun the Stage-II builder to verify DQN's final decimals.

## Reproduction commands

```powershell
.\LocalSearch\Scripts\python.exe .\LocalSearch\build_stage2_paper_comparison.py
```

The plotting script, Excel builder, archived template, final PNG/PDF figures, and final workbook have also been synchronized into the repository. The Excel builder uses `@oai/artifact-tool`; run it from a temporary workspace whose `node_modules` junction points to the Codex workspace dependency path returned by `load_workspace_dependencies`.
